import { View, Text } from 'react-native'
import React from 'react'

const Food = ({ navigation }) => {
    return (
        <View>
            <Text>Food</Text>
        </View>
    )
}

export default Food